// client/src/config.js (generado automáticamente para dev local)
// API base del backend en Render (se usa solo cuando no está MOCK)
export const API_URL = import.meta.env.VITE_API_URL || 'https://tcgconnect-server.onrender.com';
// For development UI without backend, DEV_TOKEN = 'MOCK'
export const DEV_TOKEN = import.meta.env.VITE_DEV_TOKEN || 'MOCK';